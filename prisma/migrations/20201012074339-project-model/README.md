# Migration `20201012074339-project-model`

This migration has been generated by hmoon630 at 10/12/2020, 4:43:39 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `olio`.`project` (
`projectId` int  NOT NULL  AUTO_INCREMENT,
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`title` varchar(191)  NOT NULL ,
`contents` varchar(191)  ,
`ownerId` int  NOT NULL ,
PRIMARY KEY (`projectId`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `olio`.`_participated` (
`A` int  NOT NULL ,
`B` int  NOT NULL ,
UNIQUE Index `_participated_AB_unique`(`A`,
`B`),
Index `_participated_B_index`(`B`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `olio`.`project` ADD FOREIGN KEY (`ownerId`) REFERENCES `olio`.`user`(`userId`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `olio`.`_participated` ADD FOREIGN KEY (`A`) REFERENCES `olio`.`project`(`projectId`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `olio`.`_participated` ADD FOREIGN KEY (`B`) REFERENCES `olio`.`user`(`userId`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200820024630-make-email-unique..20201012074339-project-model
--- datamodel.dml
+++ datamodel.dml
@@ -3,20 +3,32 @@
 }
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 model user {
-  userId    Int      @default(autoincrement()) @id
-  createdAt DateTime @default(now())
-  password  String
-  email     String   @unique
+  userId              Int       @id @default(autoincrement())
+  createdAt           DateTime  @default(now())
+  password            String
+  email               String    @unique
+  projects            project[] @relation("owned")
+  participatedProject project[] @relation("participated")
 }
+model project {
+  projectId    Int      @id @default(autoincrement())
+  createdAt    DateTime @default(now())
+  title        String
+  contents     String?
+  owner        user     @relation("owned", fields: [ownerId], references: [userId])
+  ownerId      Int
+  participants user[]   @relation("participated")
+}
+
 model migration {
-  revision           Int       @default(autoincrement()) @id
+  revision           Int       @id @default(autoincrement())
   name               String
   datamodel          String
   status             String
   applied            Int
```


