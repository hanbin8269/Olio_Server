name: CI
on:
  push:
    branches: [master, develop]
  pull_request:
    branches: [master, develop]

jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
    - name: Run test code
      run: yarn jest
  
